const initialState = {
    bggUserName: '',
    firstName: '',
    lastName: ''
}

function reducer(state=initialState, action) {
    switch(action.type){
        default: return state;
    }
}

export default reducer;